import { Input } from "../ui/input";

const Search = () => {
  return (
    <Input
      type="text"
      placeholder="Search Your Products..."
      className="max-w-xs"
    />
  );
};
export default Search;
